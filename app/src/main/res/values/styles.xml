<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="RatingBar" parent="Theme.AppCompat">
        <item name="colorControlNormal">@color/colorAccent</item>
        <item name="colorControlActivated">@color/design_default_color_primary_dark</item>
    </style>

    <style name="GlassRating" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">32dip</item>
        <item name="android:minWidth">36dip</item>
        <item name="android:progressDrawable">@drawable/rating_bar_bg</item>
    </style>

    <style name="GlassRatingVector" parent="@android:style/Widget.RatingBar">
        <item name="android:minHeight">32dip</item>
        <item name="android:minWidth">36dip</item>
        <item name="android:progressDrawable">@drawable/rating_bar_bg_vector</item>
    </style>

</resources>
